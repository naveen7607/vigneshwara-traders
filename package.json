{
  "name": "vigneshwara-traders-website",
  "version": "1.0.0",
  "description": "A premium price tracker and builder material estimator website for Vigneshwara Traders.",
  "main": "server/server.js",
  "scripts": {
    "install-all": "npm install --legacy-peer-deps && npm install --prefix server --legacy-peer-deps && npm install --prefix client --legacy-peer-deps",
    "build": "npm run build --prefix client",
    "start": "node server/server.js",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client"
  },
  "keywords": [
    "cement",
    "steel",
    "price-tracker",
    "construction"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "concurrently": "^8.2.2",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.7.2"
  }
}
